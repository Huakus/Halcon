@model IEnumerable<Manager.Models.Alarmas>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ValorMaximo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Observaciones)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Relevamientos.Observaciones)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Umbrales.Observaciones)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ValorMaximo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Observaciones)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Relevamientos.Observaciones)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Umbrales.Observaciones)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdAlarma }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdAlarma }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdAlarma })
        </td>
    </tr>
}

</table>
